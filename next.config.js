/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // ❌ nada de experimental.appDir aqui (Next 13+ já usa App Router por padrão)
};

module.exports = nextConfig;
