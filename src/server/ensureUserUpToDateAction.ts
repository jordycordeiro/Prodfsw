'use server'

export async function ensureUserUpToDateAction() {
  // Temporariamente desabilitado até configurar variáveis de ambiente
  console.log('[ensureUserUpToDate] Sincronização desabilitada temporariamente')
  return { success: true, synced: false }
}